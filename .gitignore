# Lean / Lake build artifacts
/.lake/
/build/
*.olean
*.ilean
*.trace
*.hash

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.toc
paper/*.synctex.gz
paper/*.fls
paper/*.fdb_latexmk
paper/*.pdf

# Python
__pycache__/
*.pyc
.venv/

# Large local data (the OEIS dump is ~3 GB; fetch it yourself, see mining/README.md).
# Covers both a real clone and a symlink to one elsewhere.
/oeisdata
/oeisdata/
*.jsonl
instances_report.md
